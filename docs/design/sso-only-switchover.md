# 設計: SSO のみのログインへ切り替える（締め出しを起こさずに）

> 状態: **裁定待ち**。実装していない。
> 由来: 堀池さん（2026-08-15・原文）
> > 「初回のユーザーは **Admin として存在する**。そのユーザーは**割り当て解除できない**。
> > SSO を設定する時、**その Admin として誰を付与するか？を選ぶ**。
> > **それをして初めて、それ以降は Google（SSO）ログインのみになる。**」
>
> 🚨 この文書は `docs/`（**素材・提案**）。決まったら `knowledge/decisions/` に1枚起こす
> （`AGENTS.md` §8「調べた結果なら docs、だからこうするなら knowledge」）。

---

## 0. この機能の本質は「締め出し」

「それ以降は SSO ログインのみ」は、**間違えると誰も入れなくなる**状態を作るということ。
機能としてではなく、**壊れ方から設計する**。

**壊れ方（司令塔の列挙に、実測で1つ足した）**

| # | 壊れ方 | 起きたらどうなるか |
|---|---|---|
| A | SSO の設定を間違えたまま切り替えた | 誰も入れない |
| B | Admin に指定した人が SSO で入れない（アカウントが無い／メールが違う） | 誰も入れない |
| C | IdP が落ちた・証明書が期限切れ | 誰も入れない（**時間差で来る**） |
| D | 最後の管理者から admin を外した | 設定を戻せる人が居なくなる |
| **E** | **切り替えた瞬間、既存のセッションだけが生きている** | **そのタブを閉じた時点で誰も入れない**（🚨 下記） |

🚨 **E は「切り替え直後は普通に使えてしまう」ので、いちばん気づきにくい。**
既にログイン済みのセッション（`directus_sessions`）は切り替えの影響を受けない。
**画面は動き続け、翌朝に誰も入れなくなる。**

---

## 1. 現状（実測。**推測なし**）

### 1.1 管理者の作られ方

    lib/settings/service.ts:555-587
      "Administrator" ポリシー（admin_access: true）と
      LOCAL_ADMIN_EMAIL = "local-admin@localhost" の利用者を作る

### 1.2 🚨 管理者を失う経路は **3本**（1本ではない）

探し方: `grep -rn "admin_access" lib app` と `grep -rn "directus_access" lib app | grep -iE "del|remove|revoke"`。

| # | 経路 | 実体 |
|---|---|---|
| ① | **割り当てを消す** | `lib/admin/permissions-api.ts:372` `deleteAccess(id)` |
| ② | **ポリシーの admin_access を false にする** | 同 `:240` `updatePolicy` |
| ③ | **ポリシーごと消す** | 同 `:246` `deletePolicy` |

🚨 **①だけを塞ぐと、②と③が開いたまま残る。** 3本とも同じ判定を通す必要がある。

**利用者そのものを消す・止める経路は無い**（`app/api/users/route.ts` は **GET だけ**。
`export async function` は GET のみ・実測）。**将来 DELETE を足す人は、ここに4本目を足すことになる。**

### 1.3 いま無いもの（「見ていない0」ではなく「探して無い0」）

| 探したもの | 探し方 | 結果 |
|---|---|---|
| 最後の管理者を守る仕組み | `grep -rniE "last.?admin\|only.?admin\|最後の管理者" lib app` | **0 件** |
| SSO 設定後にパスワードを閉じる仕組み | `grep -rn "enabled" lib/auth/saml/*.ts` を読み、`config.enabled` は**入口の可否にしか使われていない** | **0 件** |
| 「SSO で1回入れた」の記録 | 同上 | **0 件** |

---

## 2. 🚨「割り当て解除できない」の読み

**私の読み: 「最後の管理者は外せない」（＝後者）。** 根拠:

1. **前者（初回ユーザーが永久に Admin）だと、その人が辞めたとき詰む。**
   `directus_users` に削除の経路が無い以上、**行は残り続ける**。
   「永久に Admin な、もう居ない人」が1人固定される。
2. 堀池さんの文は **SSO の切り替えとセットで語られている**
   （「SSO を設定する時、その Admin として**誰を付与するか**を選ぶ」）。
   **付与先を選び直せる**という前提が同じ文の中にある。
   → 初回ユーザーが不変なら「誰を付与するか選ぶ」に意味が無い。
3. 守りたいのは**特定の個人**ではなく「**設定を戻せる人が必ず1人は居る**」という状態。

🚨 **ただし断定はしない。** 「初回ユーザーは特別扱い」という読みも文面から可能で、
**どちらかで作ると後から変えるのは高い**（片方は migration と運用手順に波及する）。
→ **堀池さんに確認する価値がある。** 司令塔へ判断ボード行きを依頼する。

**確認したい問い（そのまま使えます）**:
> 「最後の管理者を外せない」でよいですか。それとも「**初回に作った人だけは、他に管理者が居ても外せない**」ですか。
> 前者だと、初回の人が辞めても他の管理者へ引き継げます。後者だと、その人の行が永久に残ります。

---

## 3. 提案

### 3.1 切り替えの条件は「設定した」ではなく「**入れた**」

🚨 **`saml.enabled = true` を保存しただけでは切り替えない。**
**指名した Admin が、実際に SSO で1回ログインできたこと**を条件にする。

    状態は3つ:
      password_only        … いま（既定）
      sso_ready            … SSO の設定は入った。**まだパスワードも使える**
      sso_only             … パスワードを閉じた

    sso_ready → sso_only の遷移は、**画面の操作では起こさない**。
    「指名された Admin が SSO で入った」ことを**サーバが観測した時だけ**進む。

  ・観測の記録先は **`ohmycms_saml` に1列**（例 `verified_admin_user` / `verified_at`）。
    🚨 **`saml.enabled` と混ぜない**（`config.ts:92` が既に
    「**enabled と『実際に動かせる状態』は別**」と書いている。**その区別をもう1段深くするだけ**）
  ・これは**壊れ方 A と B を同時に塞ぐ**。設定間違いも、Admin が入れないことも、
    **切り替える前に必ず現れる**
  ・🚨 **今日ずっとやってきた「宣言ではなく実測」を、製品の安全装置として使う形**
    （司令塔の叩き台どおり。私からの変更なし）

### 3.2 非常口は**残す**。ただし既定は閉じる

**残す**。理由は**壊れ方 C（IdP が落ちる・証明書切れ）が自分たちの操作と無関係に起きる**から。
SSO だけにすると、**製品の可用性が他社の稼働に完全に従属する**。

    OHMYCMS_ALLOW_PASSWORD_LOGIN=true   … 既定 **false**（未設定なら閉じている）

  ・🚨 **DB ではなく環境変数**にする。DB に置くと、**入れない状態では変えられない**
    （入れないから開けたいのに、開けるには入る必要がある、という循環）。
    環境変数なら**サーバに触れる人**が開けられる
  ・🚨 **開いたことが分かる形にする**: 起動時に警告を出す
    （`app/api/auth/dev-login/route.ts:10-17` が**既に同じ形**をしている。**それに倣う**）
  ・🚨 **既定値を `pass132` のような公開値にしない**
    （`.temp/2026-08-13/auth-state.md` §3.3 の実事故。**リポジトリに書かれた値は公開値**）

### 3.3 最後の管理者を外せなくする（🚨 3経路すべて）

**判定**: 「この操作のあと、`admin_access` を持つポリシーに割り当てられた
**active な利用者**が 1 人以上残るか」。残らないなら **409 で拒否**。

  ・🚨 **`local-admin@localhost` を数に入れない**（§3.4）。
    数に入れると「人が0人でも通ってしまう」
  ・🚨 **判定と更新の隙間を消す**。`SELECT ... FOR UPDATE` で対象行を掴んでから数える
    （`lib/auth/otp.ts` の `verifyLoginCode` で使った形。
    **`SKIP LOCKED` にしない**——あれは10並列で全部すり抜けて上限が効かなかった実測がある）
  ・**3経路（①割り当て削除 / ②ポリシーの admin_access を落とす / ③ポリシー削除）で同じ関数を通す**

### 3.4 `local-admin@localhost` の扱い

**残す。ただし「セッションの持ち主」以上の意味を持たせない。**

  ・理由: `directus_sessions.user` が **NOT NULL** で、
    パスワードだけで入る経路（`/api/auth/setup`）には**人のアカウントが無い**
  ・🚨 **画面に出さない**（`lib/admin/user-label.ts` が既に弾いている。**この保護を外さない**）
  ・🚨 **管理者の頭数に数えない**（§3.3）。**実在の人へ引き継がない**——
    引き継ぐと「画面に出してはいけない行」が人のアカウントと混ざる

### 3.5 🚨 壊れ方 E（既存セッション）への備え

**`sso_only` へ移った時点で、パスワード経由で作られたセッションを切る。**

  ・そうしないと「**切り替えた直後は普通に使えて、翌朝に誰も入れない**」という
    **いちばん気づきにくい壊れ方**が残る
  ・切り替えを**その場で検証可能にする**効果もある（切った直後に SSO で入り直せるかを見る）
  ・実装の目印: `directus_sessions` に「どの経路で作られたか」が要る。
    🚨 **いま無い**（実測: `lib/auth/sessions.ts` の `issueSession` は経路を持たない）。
    **列が1本要る**——これは §3.1 の列と**同じ migration に入れる**

---

## 4. 受入（**否定側から**書く。実装時にそのまま使う）

| # | 壊れ方 | どう止まるか（**測り方**） |
|---|---|---|
| A | 設定を間違えて切り替え | `sso_only` へ**進めない**。SSO ログインが失敗する限り `verified_at` が入らない。**測り方**: わざと証明書を壊し、切替 API が **409** を返すこと |
| B | 指名した Admin が SSO で入れない | 同上。🚨 **別の人が SSO で入れても進まないこと**（`verified_admin_user` が**指名した人と一致**するかまで見る） |
| C | IdP が落ちた | **非常口**（§3.2）が開けられること。**測り方**: `OHMYCMS_ALLOW_PASSWORD_LOGIN=true` で `/api/auth/setup` が **200**、未設定で **403** |
| D | 最後の管理者を外す | **3経路すべてで 409**。🚨 **管理者が2人居るときは成功すること**（正常側の対照。ガードだけ見ると「全部拒否」でも緑に見える） |
| E | 既存セッションだけ生きている | 切替後に**パスワード経由のセッションが無効**。**測り方**: 切替前に取った cookie で `/api/auth/me` が **401** |

🚨 **すべて「要求の本数・ステータスコード」で測る。画面の見た目で判定しない。**
🚨 **検証は使い捨ての環境で**（`:3101`〜`:3103` は**同じ DB を見る**。
`.temp/2026-08-13/auth-state.md` §4。**本番の SSO 設定とロールを実験で変えない**）。

---

## 5. 決めてほしいこと（実装前に）

1. **§2 の読み**（「最後の1人は外せない」か「初回の人は永久に Admin」か）— 堀池さんへ
2. **§3.2 の非常口を残すか**（私は**残すべき**と考える。理由は可用性が他社に従属するため）
3. **§3.5 の既存セッションを切るか**（私は**切るべき**。切らないと E が残る）
4. 列を足す先（`ohmycms_saml` に 2 本 + `directus_sessions` に 1 本）で良いか

## 6. やらないこと（この設計の範囲外）

  ・SSO の設定画面そのものの作り直し（`saml-settings-manager.tsx` は既にある）
  ・Google 以外の IdP を増やす話
  ・パスワードそのものの廃止（**非常口として残す**のが §3.2）

# 設計: 人を何で識別するか（メールを必須でなくす）

> 由来: 2026-08-16 堀池さんの回答 282「**A: 偽のメールもやめる（識別子を別のものにする）**」。
> 注記（**原文**。要約しない）:
> 「**初回のログインは環境変数でいい認識。なので、めあどはいらない。
> SMTP の設定をしてはじめて OTP が使えるようになり、SSO を設定したら Google 認証。
> この２つは共存できるようにする。**（外部のライターに記事をお願いしたりするので）
> その場合、**メールアドレスを登録しておけば（登録＝招待で招待メールが飛ぶ）、
> ログイン画面で入力して OTP が届く。**」
> 🚨 **実装しない。提案のみ**（司令塔の指示）。
> 関連: `sso-user-provisioning.md`（許可リスト） / `recovery-code.md` §6（251-B）

## 0. 🚨 読み違えやすいので、先に

**「メールをやめる」ではありません。**

    ❌ メールを無くす           … **OTP も招待も消える**方向。原文と違う
    ✅ **既定では要らない**       … 環境変数のパスワードで初回に入る
    ✅ **設定すれば使える**       … SMTP を設定すると OTP が選べる
    ✅ **OTP と SSO は共存**      … 外部ライターは OTP、社内は SSO、など

**つまり `email` は「識別子」から「連絡先（あれば使えるログイン手段）」へ降格する。**

## 1. 実測（2026-08-16・共有の開発 DB）

    directus_users.email               **NOT NULL** ・ 一意
    directus_users.external_identifier **nullable** ・ 一意
    provider ごと  dev 267（external_identifier **全部あり**）
                   local **2**（external_identifier **0 件**）  ← 🚨 本物の管理者2人
                   saml 1（あり）
    偽のメール     `@saml.invalid` **0 件** ／ `local-admin@localhost` **1 件**
                   （`@example.com` 244 件は検証用の実データ）

🚨 **`external_identifier` は local の 2 人に無い。** **そのままでは共通の鍵にならない。**

## 2. 🚨 いちばん大事な発見: **同じ穴が 3 つ空く**

**メールを識別子から外すと、「メールで指していたもの」が全部行き場を失う:**

    ① **内部専用の固定ユーザー**を指す手段
       🚨 **初版は「2 箇所」と書いたが、数え足りなかった**（`service.ts` しか見ていなかった）。
       **役割で割ると 5 箇所**（実測 2026-08-16。onboard の指摘で生産側と受入が見つかった）:

         **生産** `lib/settings/service.ts:615`  insert で `email: LOCAL_ADMIN_EMAIL` を作る
                                                 （🚨 **`external_identifier` は null のまま**
                                                   ＝ local の 2 人に印が無い理由）
         **参照** `lib/settings/service.ts:455 / :601`  その行を引く
         **表示** `lib/admin/user-label.ts:15`   画面に出さないための番人
         **受入** `acceptance/checks/v1-e-first-run.mjs:317`
                                                 🚨 **この email で delete している**
                                                 ＝ NULL にすると **0 件を消して緑のまま**になる

       🚨 **「引いている場所」だけ数えると、生産側と受入が漏れる。**
       **消す作業では、作る側と、それに乗っている検査も同じ数のうち。**
    ② 🚨 **「最初に作った人」**を指す手段
       251-B（最初の人の Administrator は外せない）に要る。
       **いまも無い**（`directus_users` に作成時刻の列が 0 件・実測済み）
    ③ **SSO でメールが来ない人**を指す手段
       いま `<uuid>@saml.invalid` を作って埋めている

**🚨 3 つとも「行に印を付ける」で解ける。** **別々に作ると、また偽の値が増える。**

## 3. 案

### 案A（推奨）: **`id`（uuid）を唯一の識別子にし、`email` を nullable にする**

    email      NOT NULL → **nullable**、一意は**部分一意**（NULL 同士はぶつからない）
    識別子     **`id`（uuid）**。既に主キーで、全員が持っている
    ログイン   メールがある人 … パスワード / OTP（**いまと同じ**）
               メールが無い人 … SSO / リカバリーコード / 初回の環境変数
    偽メール   🚨 **生成をやめる**（`<uuid>@saml.invalid` の行は不要になる）

  ・**引き換え**: 🚨 **画面の「その人が誰か」がメールで表せなくなる**
    → **表示名（`first_name` / `last_name`）** と **アバターの絵文字**で出す（**既にある**）
    → 🚨 **表示名も無い人**は、**何も出ない**。**そこをどうするかは決めが要る**
  ・**引き換え**: **メールで引いている 21 箇所**（実測）のうち、
    **メールが無い人にも効く必要があるもの**を見分ける作業が要る
  ・**利点**: **偽の値を作る仕組みが丸ごと消える**（§2 の③が解決）

### 案B: **`provider` + `external_identifier` の組を識別子にする**

  ・**引き換え**: 🚨 **local の 2 人に `external_identifier` が無い**（実測）。
    **埋めるには値を生成することになり、`@saml.invalid` と同じ「偽の値」を別の列で作る**
  ・**利点**: SSO の人だけを見れば、いまでも一意に引ける

### 案C: **`username` 列を新設する**

  ・**引き換え**: 🚨 **原文に無い**。堀池さんの流れは「環境変数 → SMTP で OTP → SSO」で、
    **利用者が名前を決める場面が出てこない**。**画面を1つ増やす提案になる**
  ・**利点**: メールが無くても人が読める識別子になる

## 4. 🚨 §2 への手当て（**どの案でも要る**）

**`directus_users` に、行の性質を示す印を足す。**

    内部専用か  … ① を解く（`email = LOCAL_ADMIN_EMAIL` をやめられる）
    最初の人か  … ② を解く（251-B が実装できるようになる）

🚨 **②は「これから作る環境でしか正しく付けられない」**（既に動いている環境では、
誰が最初かを後から決められない——実測済み）。**だから記録は早いほど正しい。**
**メールを外す作業と同じ migration で入れるのが安い。**

## 5. 既存データの移行

    `local-admin@localhost`（1 件）  … 🚨 **印を付けてから** email を NULL にする
                                       （印より先に消すと、その行を指す手段が無くなる）
    `<uuid>@saml.invalid`（0 件）    … いまは 0。**生成する経路を止めるだけ**
    `@example.com`（244 件）         … **検証用の実データ。触らない**
    本物の利用者                     … **そのまま**（メールは連絡先として残る）

🚨 **順序が要点**（onboard の指摘で 1 段増えた）:

    ① **印を付ける**（列を足す）
    ② **参照を印に切り替える**（`service.ts:455 / :601` ／ `user-label.ts:15` ／ 受入）
    ③ 🚨 **生産側を止める**（`service.ts:615` が偽メールを作るのをやめる）
    ④ **既存の行の email を NULL にする**

🚨 **③を飛ばすと、④のあとで「次に初期設定した人が、また偽メールを作る」。**
**onboard の受入（V1-E）は毎回まっさらな DB で初期設定を通すので、すぐ再現する。**
🚨 **逆順（④が先）だと、切り替える前にその行を指せなくなる。**

**受入の直しは onboard が持つ**（印の列が決まってから。**決まる前に直すと、
別の鍵に乗り換えるだけになる**——本人の判断）。

## 5.5 🚨 これは 282 では解けない（saml の指摘・**先に閉じる必要がある**）

**どの案を採っても、「同じ人を、次のログインで同じ行に結ぶ」は別問題として残る。**

    【測った・saml】`lib/auth/saml/client.ts:96`  identifierFormat: **null**
      理由（コメント）「メールに固定すると IdP にメールを要求してしまう。
                       Entra ID の永続 ID など、メールでない識別子を返す IdP で入れなくなる」
    【測った・saml】`metadata.ts` が受け入れると広告している形式 … **4 つ**
      unspecified / persistent / 🚨 **transient** / emailAddress

🚨 **`transient` は「毎回変わる」形式。** IdP がそれを選ぶと:

    NameID が毎回違う → `external_identifier` が毎回違う
    → 🚨 **ログインのたびに新しい利用者の行ができる**

    案B … 識別子そのものが不安定になる（**成立しない**）
    案A … `id` は安定だが、**再ログインで既存の行に結ぶ**のは
           いまも `external_identifier` で引いている（`verify.ts:197`）→ **同じく結べない**

🚨 **＝ これは識別子の設計ではなく、SAML の受け入れ形式の問題。** **分けて扱う。**
**282 より先に「transient を受け入れるか」を閉じないと、どちらの案も足元が動く。**
（対処は「広告から外す」「Assertion の Format を検査して拒否」の 2 つだが、
**どちらも `knowledge/decisions/auth-methods.md` の「メールに固定しない」に近い場所**なので、
**saml の一存では動かせない**——本人の判断）

### 🚨 いま transient が壊れていないのは、**メールが受け止めているから**

**`upsertSamlUser` の探索順（実測・`verify.ts:194〜238`）:**

    ① provider='saml' かつ external_identifier = NameID で引く
    ② 見つからない → 🚨 **`identity.email` があれば lower(email) で引く**
       → 見つかったら **その行の external_identifier を新しい NameID に付け替える**
    ③ それでも見つからない → **新しい行を作る**

🚨 **したがって、いま IdP が transient を返しても壊れない:**

    1 回目  NameID-1 で作られる（メール E）
    2 回目  NameID-2 → ①は外れる → 🚨 **②がメール E で同じ行を見つけ、付け替える**
            ＝ **同じ人が同じ行に戻る**

🚨 **そして 282 でメールを外すと、②が効かなくなる:**

    2 回目  NameID-2 → ①外れる → `identity.email` が null → **②を飛ばす**
            → ③ **新しい行ができる**  ← 🚨 **ログインのたびに増える**

**＝ メールは識別子であるだけでなく、**NameID が不安定なときの安全網**でもあった。**
🚨 **282 は「識別子を替える」だけでなく、**その安全網を外す**変更でもある。**

**これは今日、検査の側で見つけたのと同じ形**
（`check-submit-once.mjs` の「過検出の枝が、内部の壊れを受け止めていた」）。
**安全網は、外して初めて、下に何が在ったかが見える。**

🚨 **だから §5.5（transient を受け入れるか）は 282 の前提であって、独立した課題ではない。**

### 🚨 ②は安全網であると同時に、**身元を結びつける口**でもある（saml の指摘・実測で確認）

    【測った】②の問い合わせに **provider の絞りが無い**（`verify.ts:226-229`）
      → 当たりうるのは **全 provider**（いまの DB で dev 267 / **local 2** / saml 1）
      → 見つかると **provider を 'saml' に、external_identifier を NameID に書き換える**
    【測った】`password-login.ts` も **provider を見ていない**（0 件。🟢 対照: `email` は 9 件）
      → 結びついた後もパスワードでは入れる（**締め出されはしない**）

**＝ IdP が「local の管理者と同じメール」を主張すると、その行が IdP 側の身元に結びつく。**

🚨 **この危険自体は、既に `verify.ts` のコメントに書かれている**（初版から）:
> 「**IdP がそのメールの持ち主を確認している**」という前提に乗っている。
> **IdP の設定を間違えると他人の口座に入れる**ということでもある。

**saml の追加は 2 点**:

    ① **local の管理者にも当たる**（「他人の口座」は SSO 利用者どうしの話に読めていた）
    ② 🚨 **282 は、この口を副作用として閉じる**

**＝ ②を失うのは損だけではない:**

    失うもの … transient の安全網
    閉じるもの … 🚨 **メールが一致したら、provider を問わず結びつく**口

**両方を一度に手放す。** **判断はその前提で。**

### 🚨 「SSO なら本物のメールが来る」には根拠が無い（saml の自己訂正）

    【測った】provider='saml' の利用者は **1 人だけ**
    その 1 人のメールは `saml-tester@example.com` ＝ 🚨 **saml が Keycloak に設定した値**
    ＝ **標本 1 件、しかも自作**

**したがって「SSO 利用者にはメールがある」を前提にできない。**
🚨 **これは案A を後押しする**（メールが無い人が普通に居る前提で作る）。

## 6. 決めてほしいこと

1. **案A / B / C のどれか**（私の推奨は **A**）
2. 🚨 **表示名もメールも無い人を、画面にどう出すか**（案A の引き換え）
3. 🚨 **§4 の印を、282 と同じ作業で入れてよいか**（251-B が実装できるようになる）

## 7. やらないこと（この設計では）

  ・**招待の実装**（原文に在るが、識別子が決まってから）
  ・**OTP と SSO の共存**の実装（同上。**いまも両方在るので、壊さないことだけ確認する**）
  ・**`username` の UI**（案C を採らない限り不要）

## 8. 🚨 回答が来たら、こう着手する（**どれが良いかは書かない**）

**判断が止まっているだけで、設計は両方向とも書ける。**
**回答が来たら、当てはまらない側を消すだけで着手できる状態にしておく。**

### 8.1 設問1（案A / B / C）— 🚨 **着手までの距離が違う**

**これは「どれが良いか」ではなく、回答後に何本の設問が残るかという事実。**

    案A … 🚨 **回答が来た時点で着手できる**（残る設問 0）
    案B … 🚨 **もう 1 問要る**（`external_identifier` を local の 2 人にどう埋めるか）
          埋める＝値を生成する＝ **§2③（偽の値）をこの列で作り直す**ことになるので、
          「何を入れるか」を決めないと 1 行も書けない
    案C … 🚨 **もう 1 問要る**（利用者が `username` を決める画面の仕様）
          §7 で「案C を採らない限り不要」としたものが、採った瞬間に必要になる

**案A で来た場合の手順**（§5 の順序をそのまま使う）:

    ① migration 1 本  email を nullable + 部分一意 ／ §4 の印（設問3が「はい」なら同じ本に）
    ② 参照の切り替え  メールで引いている箇所を、**メール無しにも効く必要があるか**で仕分け
                      （既知: `service.ts:455 / :601` ／ `user-label.ts:15` ／ 受入 `v1-e-first-run.mjs:317`）
                      🚨 **着手する人は、この数を信じずに数え直すこと。**
                      §3 の「21 箇所」は 2026-08-16 に**別の定義**で数えたもので、
                      **この手順の「切り替えが要る箇所」とは母集合が違う**。
                      **数え直すときは、当たった 1 件の生の行を必ず出す**
    ③ 🚨 生産側を止める `service.ts:615` が偽メールを作るのをやめる
    ④ 既存 1 件（`local-admin@localhost`）の email を NULL
    受入 V1-E は onboard が持つ（②の印が決まってから）

**案B / 案C で来た場合**: 🚨 **上の追加設問を判断ボードへ返すところまでが最初の作業**。
**先に列だけ作ると、埋める値が決まっていない列が残る**（`@saml.invalid` と同じ形）。

### 8.2 設問2（表示名もメールも無い人を、画面にどう出すか）

🚨 **どの答えが来ても、先にやることは同じ**:

    表示を **1 箇所に集約する**（例 `displayNameOf(user)`）

**いま画面が `email` を直に出している箇所**（2026-08-16 実測・振る舞いで探した）:

    走査 133 .tsx → 🚨 **1 箇所だけ**  `components/admin/users-policy-manager.tsx:116  {user.email}`
    🟢 対照 在るはずのない `.zzzemail` … 0 件（この探し方は動いている）

🚨 **私は最初この節に「画面ごとに直接出しているので取りこぼす」と書いた。誤りだった。**
**1 箇所しかないので、取りこぼしを理由に集約するのは筋が通らない。**
**集約する理由は「これから増えるから」であって、「いま散らばっているから」ではない。**

**答えごとの差分は、その関数の中だけ**:

    「何も出さない」        … 空文字を返す（🚨 **行が空になる画面が出る**ので、一覧の見た目は要確認）
    「`id` の先頭を出す」    … `user.id.slice(0, 8)` ＝ 🚨 **機械の値を人に見せる**
    「『名前未設定』と出す」  … 辞書キーを 1 つ足す（ja / en 両方）
    「アバターの絵文字だけ」  … 既にある。文字の行が空になる点は 1 つ目と同じ

### 8.3 設問3（§4 の印を、282 と同じ migration で入れてよいか）

    「はい」 … migration **1 本**。🚨 **251-B（最初の人にリカバリーコードを必ず渡す）が
               282 と同時に着手できる**
    「いいえ」… migration **2 本**。251-B は 282 の完了待ちになる
               🚨 **②の印（最初の人）は、これから作る環境でしか正しく付けられない**（§4 実測）
               ＝ 遅らせるほど、正しく付けられない環境が増える

### 8.4 transient を受け入れるか（§5.5・**282 より前**）

    「受け入れる」   … saml が実装。**NameID が毎回変わる人**も `id` で同じ行に結べる形にする
                      → 🚨 **案A と噛み合う**（メールに頼らず結ぶ話なので）
    「受け入れない」 … `identifierFormat` を persistent に固定する。
                      🚨 **IdP 側に設定を要求する**ことになる（`client.ts:96` のコメントが
                      避けようとしていた形）。受け入れない場合、282 の前提が 1 つ減る

🚨 **どちらでも 282 は進む。** **ただし「受け入れる」なら、282 の②（参照の切り替え）に
「NameID で引いている箇所」が加わる**ので、範囲が変わる。**先に閉じるほうが安い。**

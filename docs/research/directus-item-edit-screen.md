# Directus の項目編集画面には何が在るか（oc による実測・2026-08-17）

> 由来: 堀池さんの原文（2026-08-17）:「この編集できるようその少なさは最悪。
> Direcutusをもっとしっかり参考にして。**ソースコードレベルでOpenCodeでしっかり調査させる。**」
> 対象 URL: `/admin/content/test/a391145c-…`（項目 1 件の編集画面）
>
> 調査は **OpenCode（oc1 / gpt-5.6-luna）** に、Directus のクローン
> `~/Develop/Projects/kk2/directus` を読ませて行った（**編集していない**。`git status` 出力なし・
> `git diff --exit-code` が 0 を実測）。
>
> 🚨 これは**素材**（調べた結果）。「だからこうする」は `knowledge/` 側に決めてから書く。

## こちらの現状（司令塔が画像から書き起こし）

```
アイテム一覧へ
test                      ← 見出し
id  ┃⧉ コピー┃
a391145c-819d-4ab2-b125-02bc7894e28e
titile
┌──────────────────────────┐
│ aaa                              │      ← 入力欄 1 つ
└──────────────────────────┘
（以下は白紙。右側も大きく空いている）
```
🚨 **保存ボタンが画像の範囲に見えない。**

## Directus 側に在るもの（すべて根拠つき・oc の報告そのまま）

| 在るもの | 根拠 |
|---|---|
| **保存メニュー**（コピーとして保存 → 新項目へ遷移） | `routes/item.vue:1245-1253` / `651-659` / `use-item/index.ts:227-236,351-375` |
| **削除**（確認ダイアログ → 一覧へ戻る） | `item.vue:1116-1147` / `694-706` |
| **アーカイブ / 解除**（archive field が在るとき。アーカイブ時は一覧へ・解除時は留まる） | `item.vue:1149-1180` / `708-721` |
| **リビジョンの右サイドバー**（履歴・日付別・比較モーダル・復元） | `item.vue:1398-1408` / `revisions-sidebar-detail.vue:57-78,89-97` |
| **版（versions）と公開フロー**（公開前比較・公開して終了・レビューなし公開） | `item.vue:1183-1234` / `1193-1232` |
| **コメントの右サイドバー**（件数取得・開いたら本文取得・入力欄） | `item.vue:1409-1413` / `comments-sidebar-detail.vue:10-13,36-60` |
| **共有の右サイドバー** | `item.vue:1414-1419` |
| **ライブプレビュー**（`preview_url` が在るとき。ヘッダーの表示切替・本文右にプレビュー・別窓・visual editing） | `item.vue:1106-1114,1301-1321,514-517,523-553` |
| **Flow の手動実行**（項目画面用の manual flow を取得して右サイドバー＋ダイアログ） | `item.vue:556-564,1420-1424` |
| **共同編集**（接続中ユーザー・フォーカス中の欄・衝突の比較と解決） | `item.vue:221-231,1097-1104,1323-1332` |

### 🚨 Directus にも「無い」もの（探した経路つき）

| 無いもの | どこを探したか |
|---|---|
| **独立した下部の操作エリア**（画面下に固定された保存バー） | `item.vue:1051-1425` / `private-view.vue:36-52` |
| **編集画面全体を display で描く仕組み** | 通常欄は `FormField` → `FormFieldInterface` → `interface-*`。display は関連一覧やテンプレート表示用。`v-form.vue:509-550` / `form-field-interface.vue:33-91` / `list-o2m.vue:102-117` / `list-m2m.vue:122-136` |

## oc が自分で挙げた未確認（そのまま残す）

- 個別 interface の中の型別フォーマット検証
- API 側の最終検証
- `PrivateView` 配下の全サイドバー実装の細部
- 参照専用なので**意図的な破壊テストは未実施**

## 使い方

**この表は「Directus に在るもの」であって、「作るもの」ではない。**
🚨 AGENTS.md / DESIGN.md の原則どおり、**「Directus に在るから作った」は理由にならない**。
どれを作るかは、**その画面が閉じる行き止まり**を 1 つずつ書いてから決める。

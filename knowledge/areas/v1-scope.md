---
type: area
title: v1 として何を約束しているか（V1-A〜E）
description: v1 の約束は V1-A SAML / V1-B ストレージ / V1-C Tiptap / V1-D メール OTP / V1-E 初回起動 の 5 本。🚨 正本は acceptance/run.mjs の V1_CHECKS で、この文書はその写しなので必ず遅れる。🚨 状態は 5 本とも ❓（受入を走らせていないため。V1-A は saml が 2026-08-16 に回したという申告のみ）。🚨 これで全部とは言えない（正本が増えれば増える）。v0.9 の 9 項目とは別の軸。
tags: [scope, acceptance, v1]
status: active
generated:
  by: agent
  at: 2026-08-17
verified: []
sources:
  - resource: "repo://acceptance/run.mjs"
  - resource: "repo://acceptance/checks/v1-a-saml.mjs"
  - resource: "repo://acceptance/checks/v1-b-storage.mjs"
  - resource: "repo://acceptance/checks/v1-cd-editor-otp.mjs"
  - resource: "repo://acceptance/checks/v1-e-first-run.mjs"
  - resource: "repo://knowledge/areas/acceptance.md"
stale_after: 2027-02-17
x_rag_okf:
  id: areas/v1-scope
  authorship: agent
---

# v1 として何を約束しているか（V1-A〜E）

> 由来: 2026-08-17。司令塔が「**残りは堀池さんの回答待ち 15 問だけ**」と配っていたが、
> **「答えが全部来たら本番に出せる状態なのか」を誰も数えていなかった**。
> 数えたところ、**「あと何が残っているか」を書いた文書が 1 本も無い**と分かったので起こした。

## 🚨 この文書は写しです。必ず遅れます

```
正本 … `acceptance/run.mjs` の **`V1_CHECKS`**（2026-08-17 時点で 298〜312 行）
```

🚨 **`run.mjs` 自身がそう書いている**（引用）:

> 「🚨 **一覧をここに 1 つだけ置き、走らせるのも検証するのも同じ一覧を使う**
> （**2 箇所に書くと、必ず片方が腐る**）。」

🚨 **したがって、この文書はその「2 箇所目」です。**
**正本が増えても、ここは自動では増えません。** 判断に使うときは `run.mjs` を引くこと。
🚨 **「これで全部」とは書きません。** 突き合わせの案は §4。

## 1. 約束（**`run.mjs` の `V1_CHECKS` を写したもの**）

| id | 約束 | 担当 | 検査 | 状態 |
|---|---|---|---|---|
| 10 | **V1-B ストレージ（S3 互換）** | storage | `checks/v1-b-storage.mjs` | ❓ |
| 11 | **V1-C Tiptap の WYSIWYG** | tiptap | `checks/v1-cd-editor-otp.mjs` | ❓ |
| 12 | **V1-D メール OTP** | auth | `checks/v1-cd-editor-otp.mjs` | ❓ |
| 13 | **V1-A SAML（SSO）** | saml | `checks/v1-a-saml.mjs` | ❓ |
| 14 | **V1-E 初回起動**（新規インストールで初期設定を終えられる） | onboard | `checks/v1-e-first-run.mjs` | ❓ |

- 🚨 **担当は各 check ファイルの冒頭に書いてある値を写した**（design が当てたのではない）。
- 🚨 **V1-C と V1-D は同じファイル**（`v1-cd-editor-otp.mjs`）。**1 ファイル＝1 約束ではない**。

## 2. 🚨 状態が 5 本とも ❓ である理由

```
🚨 この文書を書いた design は、受入を 1 回も走らせていない
   （`--docker` は共有資源を立てるため。2026-08-17 時点で schema の migration も控えていた）
＝ 🚨 **「実装が在る」と「受入が緑」は別**。ここは後者を言う欄なので、全部 ❓
```

**申告として届いているもの**（🚨 **状態の欄には入れない**）:

- **V1-A** … saml が **2026-08-16 に回した**と申告（設問 305 ア）。
  🚨 **「回した」と「いま緑」は別**なので、表は ❓ のまま。
- **V1-C / V1-D** … `v1-cd-editor-otp.mjs` の冒頭に
  「**実装より先に基準を書いている。未実装なら SKIP（PASS にも FAIL にもしない）**」と在る。
  🚨 **SKIP は緑ではない**。「まだ作っていない」と「通った」を分けること。

🚨 **状態を埋めたいなら、走らせるしかない。** 読んで埋めないこと。

## 3. 🚨 v0.9 の 9 項目とは別の軸

**同じ日に「残り」を名乗れる数え方が 4 つ在った**（2026-08-17・design が数えた）:

| 数え方 | 中身 | 状態 |
|---|---|---|
| **v0.9 の 9 項目** | `areas/acceptance.md` の受入基準 | 2026-08-13 に 8 PASS / 1 MANUAL。🚨 **その後 1000 本超のコミットが入り、未再測** |
| **v1 の 5 本** | この文書 | 🚨 **5 本とも ❓** |
| **設問** | 堀池さんへの問い | board が管理（数は動く） |
| **受領した回答** | 答えは届いたが、入っているか | board が管理（入った / 待ち / 手つかず） |

🚨 **どれも「残り」と呼べるので、数を出すときは必ずどれかを名乗ること。**
「残りは N 件」だけでは、読む人が別の軸で受け取る。

## 4. 🚨 この写しが腐らないようにする案（**まだ実装していない**）

```
`run.mjs` の `V1_CHECKS` から id と題名を抜き、§1 の表と突き合わせる検査
   ・写しに無い id が正本に在る … 🚨 この文書の足し忘れ
   ・写しに在る id が正本に無い … 🚨 約束が消えたのに残っている
```

🚨 **作るなら `run.mjs` を変えるのと同じコミットで入れること。**
片方だけ入れると、その間ずっと門が赤くなる（`save-shortcut-routes` で実際に起きた形）。

🚨 **いまは作っていない。** 理由: **状態が 5 本とも ❓ の段階で門を増やすと、
「表と正本は一致しているが、中身は誰も知らない」という緑になる**から。
**まず 1 回走らせて状態を埋めるほうが先**。

## この総覧が古くなる条件

- `acceptance/run.mjs` の `V1_CHECKS` が増減したとき（🚨 **ここは自動では追随しない**）
- 受入を走らせて、状態（❓）が埋まったとき
- 担当が変わったとき（**check ファイルの冒頭が正**）
- v0.9 の 9 項目を測り直したとき（§3 の表が古くなる）

🚨 **【書いただけ】この総覧は、古くなっても鳴らない。** 上のどれかをやった人が、ここも直すこと。

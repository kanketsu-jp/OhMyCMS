---
type: decision
title: アクションボタンは1つ。表示モードと編集モードを分ける
description: 画面の主操作を「1つのボタン + chevron-down」に集約し、主ボタンはモードで変わる。表示モードと編集モードを分ける方針と、その対象・境目・抜け道を決める。例外は理由を書いて残せば認める。
tags: [design, ui, components]
status: active
generated:
  by: rag-okf
  at: 2026-08-16
verified: []
sources:
  - resource: "repo://apps/studio/components/admin/page-action.tsx"
  - resource: "repo://apps/studio/components/ui/button.tsx"
  - resource: "repo://knowledge/decisions/ui-placement-by-frequency.md"
stale_after: 2027-02-16
x_rag_okf:
  id: decisions/action-button-and-edit-mode
  authorship: agent
---

# アクションボタンは1つ。表示モードと編集モードを分ける

> 由来: 2026-08-15 堀池さん（原文）:
> 「**アクションボタンは一つのボタン＋右に「chevron-down」アイコン。これを押下すると
> オプションが表示される。一つのボタンはその時で変わる。編集中では「保存」など。
> `/admin/profile` などでは「名前を保存」としているが、それは廃止。
> アクションボタンで「編集する」を押下する。全てにおいて基本は編集モードと表示を分ける**」

## TL;DR

- **主ボタンは画面に 1 つだけ。** 右に `chevron-down` を付け、**押すとオプションが出る**
- **主ボタンはモードで変わる。** 表示中 →「編集する」／編集中 →「保存」
- **「〇〇を保存」のような固定文言は廃止。** モードで変わるので、名前を持たせない
- **既定は、表示モードと編集モードを分ける**
- **例外は認める。ただし「なぜ分けないか」を 1 行書いて残す**（§7）

---

## 1. 形

```
[ 主ボタン ▾ ]     ← 1 つのまとまり。右端が chevron-down
   押す           → 主アクションが走る
   ▾ を押す       → オプションが開く
```

- 🚨 **`components/ui/button.tsx` の `Button` を使う。** 素の `<button>` にしない。
  理由: base が持つ `active:not-aria-[haspopup]:translate-y-px`（押すと 1px 沈む）を継承するため。
  素の要素だと**押した手応えが色だけになり、PC では出ない**
  （`tree-connector-lines.md` §4 の「Button でないなら active は hover と同じか、より濃く」）
- **置き場所は既存の枠をそのまま使う**（新しい枠を作らない）:
  ```
  PC  #header-primary-action   （ヘッダ右）
  SP  #mobile-primary-action   （下部ナビの 3 つ目）
  ```
  🚨 **PC と SP で別の枠**。片方だけ見て「主アクションが無い」と判断しないこと
  （2026-08-15 実測: SP で `#header-primary-action` は **DOM 上 2 本・見えている 0 本**、
  同時に `#mobile-primary-action` が **見えている 2 本**。片方だけ見て 0 と誤報した）

## 2. モードの境目

| | 表示モード | 編集モード |
|---|---|---|
| 入力欄 | **読めるが変えられない** | 変えられる |
| 主ボタン | **「編集する」** | **「保存」** |
| 抜け道 | — | **「やめる」**（§4） |

**境目の定義**: **保存されていない変更を持ちうるか**。
持ちうるなら編集モード、持ちえないなら表示モード。
🚨 **「設定画面だから」のような画面の種類で決めない。** その画面で何が起きるかで決める。

### 2-1. 🚨 「変えられない」の作り方 — `readOnly` は**半分の部品にしか効かない**

**2026-08-16 実測（本物のキー／クリックを送って測った。仕様の記憶で書いていない）:**

| 部品 | `readOnly` | 実測 |
|---|---|---|
| `text` / `url` / `password` / `textarea` | 🟢 **効く** | 打っても値が変わらない |
| 🔴 `checkbox` / `radio` | **効かない** | 🚨 **属性は付き `el.readOnly === true` になるのに、実クリックで変わる** |
| 🚨 `select` | **性質が無い** | `'readOnly' in el` が **false**（**`div` と同じ**）＝ React で渡しても素通り |
| `color` | **未検証** | 🚨 OS のピッカーが開くので、この方法では測れていない |

```
🟢 対照(+) readOnly を外すと text は A → ABB（＝ 入力は届いている。測れている）
🚨 `:read-only` を対照に使わないこと … **`div` にも当たる**（測って捨てた）
```

🚨 **1 回目の測定は誤りだった。** `el.value = "B"` と**スクリプトで代入**していたので、
**`text` まで「効かない」**と出た（`readOnly` が止めるのは**人の入力**であって代入ではない）。
**全部が同じ結果になる実験は、何も言っていない。**

🚨 **したがって「表示モードでは `readOnly` にする」だけでは、画面は守れない。**
**2026-08-16 実測: §5-1 の C（まとまった設定・詳細）6 枚は、**6 枚すべて**に
`readOnly` で止められない部品が 1 つ以上ある**（select / checkbox / radio / color）。

**表示モードで部品をどう出すかは design の判断待ち**（2026-08-16 時点）。候補は 2 つ:

```
案1 部品は出したまま、止め方を型ごとに変える（text 系 = readOnly ／ 他 = disabled）
    🚨 同じ画面に 2 つの作法が混ざる／`disabled` は**焦点が当たらず値を読めない**
案2 表示モードでは**部品を出さず、値を文字で出す**
    🚨 作法が 1 つになるが、**入力欄も全部そうする**ことになる（profile も作り直し）
```

### 2-2. 🚨 「やめる」は、入れた値を**捨てなければならない**

**2026-08-16 実測（3/3 再現）で見つけた不具合（`profile` にあった。`5c0e643` で修正）:**

```
編集する → 欄に入力 → やめる
  🚨 表示モードに戻っても **入れた値が画面に出たまま**（readOnly=true / 値は入力したもの）
  ＝ **表示モードが、保存されていない値を「保存済みの値」として見せる**
  さらに「編集する」を押し直すと、捨てたはずの値が **生き返る**
```

**原因**: 欄が uncontrolled（`defaultValue`）なので、**`readOnly` にしても DOM の値は残る**。
**直し**: `<form key={formKey}>` の `key` を増やして作り直し、`defaultValue` を引き直す。

🚨 **uncontrolled な欄を持つ画面は、全部この形で壊れる。** 新しく「やめる」を置くときは必ずこれを入れる。
🚨 **壊していないことも測ること**: 作り直したあとに保存が発火するか
（実測では `/api/auth/me PATCH` が出た。送信は横取りして止め、🟢 横取り自体が効くことも対照で確認）。

## 3. ▾ の中に入れるもの

```
✅ その画面で **たまにしか使わない** 操作
✅ 🚨 **破壊的な操作**（削除など）— 誤って押されないよう、必ず ▾ の中
❌ その画面の**主目的**（それは主ボタン）
```

- 堀池さん 2026-08-15（283 A）: **コレクション画面の「削除」は ▾ の中へ。主ボタンは別のもの**
- 🚨 **主ボタンが 2 つ並んでいる画面は、頻度で主を決める**
  （`ui-placement-by-frequency.md` と同じ判断軸）

**実測（2026-08-15・PC 25 ページ）: 主アクションが 2 本並ぶ画面は 2 枚**

| 画面 | 現状 | 決め方 |
|---|---|---|
| `/admin/files` | 「ファイルを追加」「フォルダを作る」 | 主＝**ファイルを追加**／▾＝フォルダを作る |
| `/admin/collections/<c>` | 「フィールド追加」「コレクション削除」 | 主＝**フィールド追加**／▾＝**削除**（283 A） |

🚨 **`/admin/files` の主を「ファイルを追加」にした根拠は推測です。**
**押された回数を測る仕組みが無い**（ログを取っていない）。
理由: フォルダは入れ物で、ファイルはその中身なので、中身を足す回数のほうが多いだろう、という推測。
**測れるようになったら差し替えること。**

## 4. 編集モードから抜ける道

🚨 **「やめる」を必ず置く。** 置き場所は **▾ の中ではなく、主ボタンの隣**。

理由: **抜け道を隠すと、変更を捨てたい人が保存してしまう**。
▾ を開かないと出てこないものは、**急いでいる人には無いのと同じ**。

## 5. 対象（実測）

### 5-1. 🚨 2026-08-16 に採り直した（こちらが最新）

**母数 26 ルート / 🚨 対象 17 / 入力 0 が 9 / 🚨 測れていない 0。**

```
🚨 対象（表示モードが要る）17:
  A 作る画面 5 … collections/new ／ files/new ／ files/new-folder ／
                 collections/<c>/fields/new ／ content/<c>/new
  B 一覧＋その場で足すフォーム 5 … labels ／ settings/{agents,policies,roles,users}
  C まとまった設定・詳細 6 … settings/{general,sso,storage} ／
                             collections/<c> ／ files/<id> ／ settings/policies/<id>
  D 部分的に実装済み 1 … profile（**名前欄は分けた。言語切替は §7 の例外**）
🟢 対照 入力 0 が 9 … admin ／ collections ／ files ／ folders ／ notifications ／
                      reports ／ settings/mcp ／ settings/version ／ content/<c>
```

🚨 **探し方を 2 通り走らせている。1 通りでは足りなかった:**

```
A 素の入力 … input / textarea / select / contenteditable
B 🚨 role  … [role=switch|combobox|checkbox|radio|slider|spinbutton|textbox]
   理由: **shadcn は Radix なので、スイッチも選択も `<button>`**。A では 1 つも見えない
実測: **A では 0 だが B では見つかった画面が 1 枚**（profile の言語切替 `combobox`「日本語」）
```

🚨 **最初は A だけで数え、`profile` を「実装済みなので対象外」と報告した。誤りだった。**
**「対象が無い」に見えたのは、探し方が 1 通りしか無かったから**（＝「見ていない 0」）。
**「無い」と結論する前に、別の探し方を 1 つ試すこと。**

🚨 **5-2 の「15 / 25」と数が近いが、同じ集合ではない。** 突き合わせた差分:

| 差分 | 中身 |
|---|---|
| 増 | `collections/<c>/fields/new` ／ `content/<c>/new` … **5-2 で見落としていた** |
| 内訳が変わった | `profile` … 名前欄は分けたが、**言語切替は表示モードのまま操作できる**（§7 の例外） |

🚨 **この数え方の当たらないところ（申告）:**
- **`content/<c>/new` は、フィールドを持つコレクションでしか対象にならない**
  （実測: `zz_probe_actions` → 欄 4 ／ `zz_a`・`acc_748015_pl` → **欄 0**）。
  🚨 **最初 `acc_748015_pl` で測って「対象外」と出た。フィールドが 0 本だっただけ**
- **既存項目の編集 `content/<c>/<id>` は測っていない**（`new` と同型と**推測**）
- **admin で測っている**。権限で欄が消える利用者では対象が減る
- 動的 ID は一覧の先頭リンクから拾う。🚨 **最初 `/new` を拾ってしまい、詳細画面 3 本を
  取りこぼした**（`^/admin/collections/[^/]+$` は `/admin/collections/new` にも当たる）

### 5-2. 2026-08-15 の初回計測（残す。**古い**）

**「最初から編集できる」画面は 15 / 25 枚**（PC・見えていて disabled/readonly でない入力を持つ）

```
collections/new ／ collections/<c> ／ files/new ／ files/new-folder ／ files/<id> ／
labels ／ profile ／ settings/agents ／ settings/general ／ settings/policies ／
settings/policies/<id> ／ settings/roles ／ settings/sso ／ settings/storage ／ settings/users
🟢 対照 残り 10 枚は入力 0（一覧・詳細の表示だけ）＝ この数え方は「全部」を返していない
```

**保存の効き方**: 15 枚のうち **「押した瞬間に効く」画面は 0 枚**。
```
実測: fetch / XHR を横取りして「送ろうとしたか」だけを見た（送信はしていない）
🟢 対照(+) フック自身が検出できることを確認（`__probe__` を 1 件記録）
→ 疑いのあった `settings/policies/<id>` も **0 件**（行ごとのボタンで編集する形だった）
```
＝ **全画面が「まとめて保存」**。モードの意味が画面ごとに変わることはない。

## 6. 廃止するもの

```
❌ 「名前を保存」のような **固定の文言**（`profile_name_save` 等）
   → 主ボタンはモードで変わるので、**画面ごとの名前を持たせない**
❌ 本文の中に置かれた保存ボタン
   実測（2026-08-15）: **2 箇所**（`/admin/labels`「作る」／`/admin/collections/<c>`「追加」）
   🚨 どちらも「一覧＋その場で足すフォーム」なので、
      **「編集する」ではなく「追加」が主アクション**になる型。単純に寄せられない
```

## 7. 🚨 例外は認めない（2026-08-16・設問287 は **B** が最新）

**最新のご回答: B — 15 枚すべてに入れる。例外なし。**

### 🚨 297 A で「作る画面 5 枚」が例外になった（2026-08-16）

> 堀池さん（原文）: 「〜/new のような新規作成ページの場合、**そこに必要なのは保存です。
> 編集じゃない。アクションボタンの定義をより洗礼させて。**」

**【測った】「作る画面」は 5 枚**（🟢 対照 静的ルート 32 本から抽出）:
```
/admin/collections/new
/admin/collections/[collection]/fields/new
/admin/content/[collection]/new
/admin/files/new
/admin/files/new-folder
```

🚨 **287 B と 297 A は「重なっていない」のではない。重なっている。**
```
司令塔の読み … 「287 は既存を見る画面、297 は新しく作る画面。重なっていない」
🚨 実測      … **15 枚のうち 3 枚が作る画面**
                （collections/new / files/new / files/new-folder）
   残り 2 枚（fields/new・content/<c>/new）は **動的ルートなので 15 枚の測定に入っていなかった**
＝ 🚨 **297 A は 287 B の「例外」。部分集合を抜く形**
```
🚨 **「重なっていない」と読むと、15 枚の中の 3 枚に編集モードを入れてしまう。**

### 🚨 洗練した定義 — **画面の種類で、主アクションが決まる**

```
🚨 判断の順序（上から当てる）:

① **作る画面**（まだ何も無い。上の 5 枚）
   表示モードを **持たない**（表示する状態が存在しない）
   主ボタン … **保存**   ▾ … 無し（**やめるは「戻る」なので、主ボタンの隣**）

② **既存を見る画面で、変えられるものが在る**（15 − 3 = 12 枚）
   表示中 … 主ボタン **編集する**
   編集中 … 主ボタン **保存**  ＋ **やめる**（主ボタンの隣。§4）
   ▾ … **たまに使うもの・破壊的なもの**（283 A の「削除」はここ）

③ **一覧＋その場で足すフォーム**（§6 の型・5 枚）
   🚨 **「編集する」にならない。** 主ボタンは **その画面の主目的**（「作る」「追加」）
   → **①に近い**（フォーム部分は最初から入力できる）

④ **入力を持たない画面**（10 枚）
   主ボタン … その画面の主目的、無ければ**置かない**
```

🚨 **283 A と矛盾しない。** `/admin/collections/<c>` は②なので、
**主ボタンは「編集する」、削除は ▾ の中**（283 A のとおり）。

🚨 **「編集する」と「保存」は別の動詞**（堀池さんの指摘の核心）。
```
編集する … **状態を変える**（表示 → 編集）。**まだ何も保存しない**
保存     … **値を確定する**
＝ 🚨 **作る画面に「編集する」を置くと、押しても何も起きない**
   （既に編集できるので、状態が変わらない）
```
**これが「そこに必要なのは保存です。編集じゃない」の意味だと読みました。**
🚨 **違っていたら言ってください**（この読みの上に①〜④を立てています）。


> 🚨 **この節は 3 回ひっくり返っている。消さずに順番を残す**
> （消すと、次に読んだ人が「ずっとこうだった」と読み、また同じ議論をする）:
> 1. **B（例外なし）** が配られる
> 2. **A（例外は理由を書けば認める）** が 3 通一致で最新になり、B は撤回
> 3. 🚨 **2026-08-16、再び B。** A で書いた例外は**通らない**

🚨 **B に伴って止まっているもの（司令塔が堀池さんへ確認中）:**

```
「作る画面」5 枚 … collections/new ／ files/new ／ files/new-folder ／
                   collections/<c>/fields/new ／ content/<c>/new
🚨 まだ何も無いので、**表示する状態が存在しない**
   B だと **空の表示モードを出して「編集する」を押させる**ことになる
→ 🚨 **この 5 枚は司令塔の確認が出るまで着手しない。** 残り 10 枚は B で進める
```

### 7-1. 🚨 B の下で宙に浮いている 1 件（**私では決められない**）

```
/admin/profile の言語切替（Radix Select）
  選んだ瞬間に反映され、**保存前の状態を持たない**
  🚨 B（例外なし）を字義どおり当てると、表示モードでは操作できないことになる
     → **言語を変えるのに、氏名の編集モードへ入る**ことになり、意味が通らない
🚨 A のときに「例外」としてコードへ 1 行書いた（`4126e03`）。**B ではその根拠が消えた**
→ **司令塔へ上げてある。** 判断が出るまで、その 1 行は**消さずに置く**
   （消して戻す作業を 2 回するほうが危ない）
```

**以下は A のときに書いた検討。B では根拠にならないが、実測は残す:**

| 条件 | 該当 |
|---|---|
| 押した瞬間に効く**画面** | **0 件**（§5-2 の実測。fetch/XHR を横取りして確認） |
| 🚨 押した瞬間に効く**部品**（画面の中の 1 つ） | **1 件**（`profile` の言語切替） |

🚨 **2026-08-16 訂正: 「探して 0 件」は誤りだった。** 数えていたのは**画面**の単位で、
**画面の中の 1 部品**が押した瞬間に効く形を見ていなかった。

```
/admin/profile の言語切替（Radix Select）
  値を変えた瞬間に送信する（profile-settings.tsx の onValueChange → startTransition）
  ＝ **保存されていない変更を持ちえない**（§2 の境目でいうと表示モード側）
  → 🚨 **編集モードに入れない。表示モードのまま操作できてよい**
```

**そのコードに書く 1 行:**
```
🚨 表示モードでも操作できる。理由: 選んだ瞬間に反映され、保存前の状態を持たない
```

🚨 **この 1 件は「探し方を変えたら出た」もの。**
素の `select` を探す方法では見えず、`[role=combobox]` で初めて見えた（§5-1）。
**「例外が無い」と書く前に、探し方を 2 通り以上試すこと。**

**A のときに定めた「例外の書き方」（🚨 B では使わない。**新しく例外を作らないこと**）:**
```
その画面のコードに 1 行:
  🚨 表示モードを分けない。理由: <その画面で何が起きるか>
❌ 「設定画面なので」のような**画面の種類**で書かない
   → 種類で書くと、**新しい画面がどちらに入るか誰にも分からなくなる**
```
🚨 **B のいま、この書き方を使ってよいのは「7-1 の 1 件」だけ**（判断待ちのため残置）。
**新しく「例外です」と書き足さないこと。** 迷ったら**分ける側**に倒し、司令塔へ上げる。

## 8. 🚨 この規約が当たらない場面

- **cva（`class-variance-authority`）の定義そのもの** — どの要素に付くかは**使う側**が決めるので、
  「Button か」の判定は定義側ではできない。**呼び出し側で判定する**
- **入力を持たない 10 枚** — 表示モードしか無いので、モードの分離が要らない
- **一覧＋その場で足すフォーム**（§6）— 主アクションが「編集する」にならない

## 9. レビュー観点

- [ ] 主ボタンは画面に 1 つか（2 つ並んでいないか）
- [ ] `Button` を使っているか（素の `<button>` で沈み込みを失っていないか）
- [ ] PC と SP の**両方の枠**で出ているか
- [ ] 主ボタンの文言がモードで変わるか（固定の「〇〇を保存」になっていないか）
- [ ] 破壊的な操作が ▾ の中に在るか
- [ ] 編集モードに**抜け道**が在るか（▾ の中に隠していないか）
- [ ] 例外にした画面に、**なぜ分けないかが 1 行**書いてあるか（画面の種類で書いていないか）

---
type: decision
title: 検証用に見えるデータを、名乗りが無いという理由で消さない
description: 共有 DB に残る zz_ / acc_ のようなデータには「検査のために意図して常設にしたもの」が混ざる。担当者が居なくなっても残り続けるため「全員が違うと言った」は消してよい根拠にならない。消す前に knowledge/ docs/ scripts/ を grep して、リポジトリに理由が書かれていないことを確かめる手順を決める。
tags: [acceptance, testing, ops, design]
status: active
generated:
  by: agent
  at: 2026-08-15
verified: []
sources:
  - resource: "repo://knowledge/decisions/checks-must-declare-blind-spots.md"
  - resource: "repo://apps/studio/scripts/audit-surface-depth.mjs"
stale_after: 2027-02-15
x_rag_okf:
  id: decisions/permanent-fixtures-are-not-junk
  authorship: agent
---

# 検証用に見えるデータを、名乗りが無いという理由で消さない

> 由来: 2026-08-15。本番の共有 DB に検証用に見えるコレクションが 4 件見え、
> 「**全員に持ち主を聞き、名乗りが無ければ消す**」という手順で片付けようとした。
> そのうち `zz_probe_act`（= `zz_probe_actions`）は
> [checks-must-declare-blind-spots](./checks-must-declare-blind-spots.md) で
> **「常設にする」と決めたもの**だった。**10 人が全員、決定文書を読まずに消しかけた。**
> 問題は「持ち主が分からない」ことではなく、**書いてあることが読まれていない**ことだった。

## 決定

**共有 DB の行・コレクション・ファイルを消す前に、リポジトリを grep する。**

```
grep -rn "<その名前>" knowledge/ docs/ apps/studio/scripts/
```

🚨 **名乗りは「消してよい」の根拠にならない。**
**リポジトリに理由が書かれていないことを確かめて、初めて消せる。**

### なぜ名乗りでは足りないか

- **意図して常設にしたものには、担当者が居ない。** 決めた人はもうそのペインに居ないかもしれない
- **名前が「捨ててよい」に見える。** 🚨 `zz_` は「検証用＝消してよい」に読める。
  **実際には「消えると検査が再現できなくなるもの」が同じ接頭辞で並んでいる**
- **「作った」と「使った」は別。** 名乗りを所有と読むと、**他人の検証が壊れる**
  （2026-08-15、`acc_748015_pl` について 2 人が独立に「**使ったが作っていない**」と申告した。
  この区別が無ければ「誰のものでもない」と判定されて消えていた）

## 名乗るときの型

**「作った」「使った」「既定値として持っている」のどれかを書く。**
「知らない」も報告する——**全員が黙ると、消してよいのか永久に決まらない。**

## いま常設にしているもの

| 名前 | なぜ消さないか | 根拠 |
|---|---|---|
| `zz_probe_actions` | **行のあるコレクションが 1 つも無いと、行が要る検査が「測れなかった」になる。** 一度これが消え、その形の 500 が誰にも再現できない時間帯があった | [checks-must-declare-blind-spots](./checks-must-declare-blind-spots.md) |
| `acc_748015_pl` | 受入ハーネスが既定値として使っている（`--richtext-collection` の既定） | `apps/studio/scripts/audit-surface-depth.mjs` |

🚨 **この表に足すのは、常設にすると決めた人の仕事。** 足さずに常設にすると、
**次の掃除で消える**（今回まさにそうなりかけた）。

## これから常設にするものの名前

**`keep_` で始める。** 名前だけで「消してはいけない」が分かる形にする。

🚨 **既にあるもの（上の表）は改名しない。** 本番の DDL を伴い、
**改名の失敗のほうが、名前が分かりにくいことより高くつく**ため。
**表に載っていることが根拠**になる。

## まだやっていないこと（unverified）

- **DB 側から「常設」だと分かる仕組みは無い。** いまは
  **名前（`keep_`）と、この表**の 2 つだけが手掛かり
- 🚨 **常設のものが消えたことを検知する仕組みも無い。** 元の事故
  （「消えて、その形の 500 が再現できない時間帯があった」）は、**いまでももう一度起きうる**。
  検知するなら「常設の一覧が DB に在ることを確かめる `verify:` を 1 本足す」形になるが、
  **実 DB が要るので pre-commit には載せられない**。**未着手**

## アンチパターン

- ❌ 「全員が『違う』と言ったから消す」
- ❌ 名前の接頭辞（`zz_` / `test_` / `tmp_`）を「捨ててよい」の根拠にする
- ❌ 名乗り＝所有と読む（「使った」人を持ち主として扱う）
- ❌ 常設にすると決めたのに、上の表に足さない
- ❌ 消す前の grep で **0 件だったことだけ**を根拠にする
  （🚨 **実在する名前で 1 件以上出ることを対照として確かめる**。
  「見ていない 0」と「無い 0」は同じ顔で出る）

## レビュー観点

- [ ] 消す前に `knowledge/` `docs/` `apps/studio/scripts/` を grep したか
- [ ] その grep が効いていることを、実在する名前で確かめたか
- [ ] 名乗りを「作った／使った／既定値」で区別したか
- [ ] 常設にするなら、上の表に足したか / 名前を `keep_` で始めたか

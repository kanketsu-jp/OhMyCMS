# DESIGN.md — 画面の作り方（OhMyCMS）

> このファイルは **AI エージェントが毎回読む**（`AGENTS.md` から名指しで指している）。
> `.claude/rules/` には置かない——**OpenCode などが読まないため**
> （出典: https://vercel.com/blog/agents-md-outperforms-skills-in-our-agent-evals ）。
>
> 中身は **堀池さんが画面を見ながら出した指摘**を規約に直したもの。**原文を必ず併記する**
> （要約すると「そこまでは言っていない」が後で分からなくなる）。
> 由来の全文は `.temp/2026-08-17/ui-fixes-from-screen.md`（会話が消えても残る側）。
>
> 🚨 **迷ったら Directus の実物を見る**。クローンが `~/Develop/Projects/kk2/directus` に在る（読むだけ）。
> ただし **Vue なので写経しない**。構造・名前の付け方・単位の切り方だけを見て React + shadcn に置き換える。

---

## 0. 大前提

**全て Directus を見習う。UI は shadcn/ui。無いものだけ自作する。**

🚨 **ただし「Directus に在るから作った」は理由にならない。**
その画面・その部品が**閉じる行き止まり**を 1 つずつ書けること。枚数を揃えるのが目的ではない。

### 0-1. 🚨 作る前に「既に在るもの」を探す（DRY）。共通は必ずラッパーにする

> 原文（2026-08-17）:「**すべてルールかつ、DRY 思想ですでにあるものがないか？
> Layout.tsx はラッパーを用意して綺麗な共通 UI にするなど常に意識**」

**手を動かす前に、必ず 2 つやる。**

1. **同じことをする部品が既に在るかを引く。**
   引かずに作ると、**同じ役目のものが 2 箇所になり、直すとき片方が腐る**。
   実例（2026-08-17）: 接続線の実装が `components/ui/tree.tsx` に既に在り、
   `TreeItem` の使用は**リポジトリ全体で 0 件**だった。作らずに済んだ。
   実例（2026-08-17）: チャットの部品（`message` / `message-scroller` / `attachment`）が既に入っていて、
   報告のやりとり画面はもうそれで書かれていた。

2. **2 箇所以上に出るなら、その場で書かずにラッパーを作る。**
   置き場は **`components/admin/`**。中で `components/ui/` の素を使う。
   実績: `error-banner` / `list-empty` / `field-label` / `page-skeleton` / `clickable-row` /
   `panel-section` は、すべてこの形で共通化した。

🚨 **`components/ui/**` を単独で編集しない。** shadcn の生成物で、共有されている。
実測（2026-08-17）: `input.tsx` は **25 ファイル**から参照。1 ファイル直すと 25 画面に及ぶ。
**1 画面のつもりで直すと、25 画面が変わる。** 触るなら、影響件数を自分で数えて司令塔へ予告する。

🚨 **判断の軸は「どのファイルを開くか」ではなく「何画面に及ぶか」。**
（司令塔が一度この軸を取り違え、`globals.css` で全部の表に効く直しを指示して、
担当から「それは避けたい形そのものです」と正しく差し戻された。）

### 0-2. レイアウトは Layout に持たせる

> 同上（原文）:「**Layout.tsx はラッパーを用意して綺麗な共通 UI にするなど常に意識**」

画面ごとに枠・余白・見出しを組み直さない。**共通の器を 1 つ作り、中身だけ差し替える。**
新しい画面を足すとき、**その画面だけの枠を書いていたら、それは共通化の合図**。

---

## 1. 面と余白

### 1-1. 角丸を使わない（クロームは平ら）

> 原文（2026-08-17）:「Headerのデザインが破壊的に悪い。**角丸はいらない。フラットに。**」

ヘッダー・サイドバーなど**画面の枠にあたる部分**は、角丸を付けず、区画を隙間なく並べる。
角丸のピルを横に散らさない。

### 1-2. パディングは親ではなく中の要素に持たせる

> 原文（2026-08-17）:「パディングがあるので、余白が気持ち悪い。**パディングは親じゃなく中の要素に持たせる。**」

節の器（アコーディオンの外側など）に padding を置かない。中の要素が自分の余白を持つ。
器に置くと、中身が無いときに**空の帯**が残る（→ 1-4）。

### 1-3. divider と space-y を使う。ただし「何を分ける線か」を区別する

> 原文（2026-08-17）:「アコーディオンの見出しの背景色も一緒なので境目がわからない」
> 「**Divier、space-yをちゃんと活用**」

🚨 **同じ「線」でも役目が違う。取り違えると 2 日前の指示を壊す。**

| 線の役目 | 可否 | 根拠 |
|---|---|---|
| **節の中で、見出しと中身を分ける** | ✅ **使う** | 2026-08-17「境目がわからない」 |
| **項目どうしの間を分ける** | ❌ **使わない** | 2026-08-15「アコーディオンの上にある divider はいらない。**divider は明確に分けるものなので、今の位置にあると設定は別の要素と考えてしまう**」 |

🚨 `components/ui/accordion.tsx` は開けない。**「既定が罫線を持たない」ことが守り手**。
線を足すなら**呼び出し側**で足す。

### 1-4. 中身が 0 件のとき、器と線を残さない

> 原文（2026-08-17）:「謎のボーダーがあるので削除」
> 原文（2026-08-17）:「項目一覧がゼロのときはアコーディオン自体を表示しないように」

🚨 **ただし「0 件だから消す」ではなく「なぜ 0 件か」を先に測る。**
何かが入るはずなのに 0 件なら、**消すと二度と出ない**。正しいのは削除ではなく**出し分け**。


### 1-5. 0 件のときは、表そのものを出さない

> 原文（2026-08-17）:「ロールはまだありません。→ **ないならそもそもデータテーブルを出すな。**」

中身が 0 件なのに、**列見出しだけの空の表**を出さない。案内文だけにする。
🚨 §1-4（空の器と線を残さない）と同じ考え方。**器が先に出て、中身が後から来る形を作らない。**

### 1-6. 「一覧」と「作る」のあいだに境目を作る

> 原文（2026-08-17）:「**一覧と作成の境目がないので、つくれ。**」
> 原文（2026-08-17・別画面）:「**すべて、一覧と作成などの境目がない**」

一覧の下に作成フォームが**同じ面のまま続く**形にしない。
🚨 「すべて」と言われている。**1 画面だけ直さない。**
同じ形の画面を**全部数えてから**直す（実例: `/admin/settings/roles` / `/admin/files/labels`）。


### 1-7. 右サイドバーの文字を小さくしない

> 原文（2026-08-17）:「右再度バーの文字が小さすぎる」（＝ 右サイドバー）

説明文だからといって一段小さくしない。**まとまった量を小さい字で詰めない。**


### 1-8. 欄は、境界が分かること（表示モードでも・空のときも）

> 原文（2026-08-17）:「**フィールドの枠がないので、わからない。**」
> 原文（2026-08-17）:「未入力のフィールドは背景色がないため分かりにくいです。
> 点線にする、プレースホルダーを表示する、または仮の値をダミーとして入れておくなどして工夫して」

ラベル・値・説明が枠なしで縦に流れると、**どこからどこまでが 1 つの欄か分からない**。
🚨 **値が空のときが最悪**。ラベルと説明のあいだが**完全な空白**になり、欄が在ることすら分からない
（実例: `/admin/settings/storage` のエンドポイント / バケット / リージョン）。

→ **表示モードでも欄の境界を出す**（枠・地の色・区切りのいずれか）。
→ **空のときは「未設定」等を出す**。何も無い状態にしない。


### 1-9. 🚨 区切り線の前後に余白を取る。線と見出しをくっつけない

> 原文（2026-08-17）:「『更新の確認』の真上にボーダーがあり、**余白という概念がないので、
> ちゃんと考えて。ルールにも追加。どう考えても近すぎる**」

**線は「分ける」ためのもの。分けた先にすぐ文字が来ると、分けたことにならない。**

- **線の下（次の節の見出しまで）に、行間より明確に大きい余白を置く**
- **線の上（前の節の最後の要素から）にも同じだけ置く**
- 🚨 **上下を非対称にしない**（片側だけ空いていると、線がどちらの節のものか分からなくなる）

🚨 これは §1-3（divider と space-y を活用）の**続き**。
線を足すこと自体は求められているが、**足しただけでは足りない**。
**「線を引く」と「余白を取る」は 1 組**で、片方だけやると今回の指摘になる。

💭 実装の目安: 節の区切りは `space-y` を器に持たせ、線は**その間隔の中央**に置く。
線に `margin` を直接足して寄せない（→ §1-2 パディングは中の要素に持たせる、と同じ考え方）。


### 1-10. 空のときは「無い」で終わらせない。次にできることを隣に置く

**「まだありません」だけを出して終わりにしない。** その画面で**本当にできること**を、
**空の文の隣（本文の中）**に置く。

🚨 **ヘッダーに主ボタンが在っても、本文の中にもう一度置いてよい。**
空の画面では本文しか見るものが無く、**ヘッダーは目に入らない**。
（🚨 これは実測ではなく見立て。人の視線は測れない。ただし
「**在る場所が画面によって違う**」ことは実測で確かめた。下記。）

実測（2026-08-17・pages）:
```
ListEmpty を使っている画面 …………………… 16 本
そのうち空の文の隣に「次にできること」が在る … 🚨 1 本だけ
   （app/(admin)/admin/content/page.tsx の「コレクションを作る」）
＝ 残り 15 本は「ありません。」で終わっている
🟢 対照 空でない画面（settings/ai?tab=mcp）… 本文の中の操作 4
```
🚨 **操作が無いわけではない。** 例: `/admin/reports` の「報告する」は**ヘッダーに在る**。
問題は **在る場所が画面によって違う**こと。初めて触る人は、
**空の本文を見て「ここでは何もできない」と読む。**

- **権限が無い人には出さない**（できないことを勧めない）
- **§1-5（0 件のときは表を出さない）と 1 組**。表を消したあと、そこに何を置くかがこれ


### 1-11. 居座る画面には「この画面は何か」を出す。通りすがりの画面には出さない

> 由来: 2026-08-17・auth が「初めて触る人の目で 1 周」して見つけた。堀池さんの指摘ではない。

右サイドバーの「概要」が、**画面によって在ったり無かったり**していた。実測:
```
page-meta.ts の経路 30 本 … 概要が出る 17 ／ 🚨 出ない 13（合計 30 ＝ 母集合と一致）
到達できる 4 本を実画面で確認 … /admin/profile ／ collections/new ／ files/new ／ files/new-folder
   （右サイドバーを開いても「API・MCP」しか無い）
```
これは**開いた瞬間の見え方**に出る:
```
概要が在る … /admin/files 2 節・開いている 1（概要）
概要が無い … /admin/content/<c> 4 節・開いている 0 ／ /admin/profile 1 節・0
＝ 初めての人が右サイドバーを開くと、「説明が出る」「見出しだけ並ぶ」に割れる
```

**決め（2026-08-17・司令塔）**:
- **居座る画面**（その画面で作業する。profile / content の一覧 / コレクションの詳細 / ファイルの詳細）
  … **説明を足す**
- **通りすがりの画面**（`*/new` など、入力して離れる）… **足さない**
- 🚨 **「最初に在る節を既定で開く」はやらない**。`/admin/profile` は唯一の節が「API・MCP」なので、
  **開いた瞬間にプロンプトの塊**が出る（＝ 一律の規則が、ある画面で必ず悪くなる）

🚨 **動的な名前が入る画面**（`/admin/content/[collection]` 等）は、
静的な文が書きにくい。**その場合は「何ができる画面か」を書く**（名前を書こうとしない）。

🚨 **「足す」の意味は「`descriptionKey` を持つこと」まで**（2026-08-17・司令塔が決めた）。
本文にも重ねて出すことは**求めない**。理由:
```
descriptionKey を読んでいるのは components/admin/page-info-panel.tsx の 1 本だけ（実測）
＝ 本文にも出すと、同じ文言の出口が 2 つになる。DRY を壊す
🚨 「右パネルを開かないと読めない」のは事実だが、それは
   §1-11 ではなく「右パネルを既定で開くか」という別の問いになる
```
どうしても本文にも出すなら、**同じ鍵を読む**形にすること（文言は 1 つ・出す場所が 2 つ）。


### 1-12. 🚨 編集できる欄と、できない欄を見分けられるようにする

> 原文（2026-08-17）:「編集モードなのにどこが編集できるフィールドなのか全くわからない」

実例 … 「アイテム編集」で `id`（読み取り専用）と `title`（編集できる）が、
**同じ薄いグレーの箱**で並んでいた。

🚨 **§1-8（表示モードの欄に地の色）を入れた副作用**。
表示モードに地の色を敷いたので、**編集モードでも同じ見た目**になった。
→ **編集できる欄は、編集できると分かる見た目にする**（枠・地の色・カーソルのいずれか）。
🚨 **1 つの規約を入れると、別の状態が巻き添えになる**。入れたら**他の状態も見る**。

---

## 2. 操作（ボタン・メニュー）

### 2-1. オプションがあるなら、必ずボタングループにする

> 原文（2026-08-17）:「**オプションがあるならかならずボタングループにする**」
> 原文（2026-08-17）:「一つのアクションを固定するのではなく、『ファイルを追加』をメインとし、
> そのオプションとして『フォルダを作る』が選べるようなボタングループの形式に」

主ボタンと `▾` が**隙間なく繋がる**形にする。離して並べない。
🚨 これは**一般則**。1 箇所直すときは、`▾` を持つ操作を**全部数えて**列挙してから直す。

### 2-2. 操作は横幅を確保する。小さくしない

> 原文（2026-08-17）:「**アクションはちゃんと横幅をかくほ**」「**ボタンが小さすぎる**」

### 2-3. 開くメニューに最小幅を持たせる（日本語は縦に潰れる）

> 原文（2026-08-17）:「このように**文字が縦長になる**のも最悪」

`▾` の幅（32px 程度）にメニューが引きずられると、日本語が**1 文字ずつ縦に折り返す**。
メニューには最小幅を持たせる。

🚨 **浮くメニューが下の要素に載るのは、直さない**（2026-08-17・司令塔が決めた）。
Radix の既定であり、閉じれば下の操作は押せる。**操作は塞がれていない。**
避けようとすると「項目数」や「行の位置」で崩れるので、次に同じ指摘が出る
（実測: 表の最後の行で開くと、ページ送りの「前へ」に 1 件重なる）。

### 2-4. ショートカットは**ツールチップ**で見せる。バッジで置かない

> 原文（2026-08-17・後）:「**ショートカットバッジは窮屈なので、すべて廃止。
> 代わりにツールチップにする。**」

🚨 **これは同じ日の 30 分前の指示を反転させたもの。経緯を残す。**
```
先 … 「ショートカットキーも表示しながら」          ← 文字で指示された
後 … 「バッジは窮屈なので、すべて廃止。ツールチップに」 ← 実物を見て変わった
```
**ショートカットを見せること自体は続く。見せ方だけが変わった。**
🚨 「前の指示が誤りだった」ではない。**作ってから変わることが在る。**

→ だから **広く効く見せ方を作るときは、1 箇所だけ作って見せてから広げる**。
　 全画面に配ってから反転すると、剥がす作業が生まれる。

### 2-5. アクションは 2 つまで ＋ オプション。基本は 1 つ

> 原文（2026-08-17）:「**アクションボタンは２つまで＋オプションにします。基本は一つ。**」

主ボタン 1 つが基本。多くても 2 つ（例: 「やめる」「保存」）。
**それ以上は `▾` の中へ入れる**（→ §2-1）。

### 2-6. 高さはヘッダーいっぱい。パディングで縮めない

> 原文（2026-08-17）:「パディングをなくし、各ボタンの高さがヘッダーいっぱいになるように。
> アイコンのサイズやクリック範囲が広がるはずです」

### 2-7. メニューの項目は、位置と色を揃える

> 原文（2026-08-17）:「**報告するだけずれている**」「**色も合わせる**」

同じメニューの中で、アイコンの左端と文字の濃さを揃える。
🚨 薄いのが「押せない」の表現なら、**色ではなく「なぜ押せないか」が本当の指摘**。先に測る。


### 2-8. 入力欄に見えるものは、打てること

> 原文（2026-08-17）:「検索窓は検索窓としての機能ではない。モーダルを開くだけ。
> ならば、検索する　みたいなボタンにして。もしくはちゃんと機能を追加して。
> どちらにせよいまは背景色と同化してわかりずらいので修正」

**見た目と、できることを食い違わせない。**
入力欄の形をしていて打てないなら、**ボタンの見た目にする**か、**打てるようにする**。
あわせて、**地の色と同化させない**（在ることが分かること）。


### 2-9. 🚨 直らないものを「もう一度お試しください」と言わない

> 由来: 2026-08-17・schema が「途中で失敗したとき」を通して見つけた。

```
いまの文言 …「処理できませんでした。時間をおいてもう一度お試しください。」
実際 ……… 🚨 主キーの欄が消えているので、**時間をおいても永久に直らない**
```

**再試行で直るものと、直らないものを分ける。**
- 直るもの（通信・一時的な混雑）… 「もう一度お試しください」でよい
- 🚨 直らないもの（設定が壊れている・権限が無い・対象が消えた）
  … **何をすれば直るか**を書く（「主キーを作り直してください」「管理者に権限を頼んでください」）

🚨 **「失敗しました」だけで終わらせない。** 失敗の画面で見るのは 3 つ:
```
① 何が起きたか分かるか
② その人が次に何をすればよいか書いてあるか
③ 🚨 その画面から出られるか（覆いが残って何も押せない、が最悪）
```

### 2-10. 🚨 壊せる操作は、API で止める（画面で隠すだけにしない）

```
実測（2026-08-17）… DELETE /api/fields/<c>/<主キー> が **200**（成功する）
   id 列 0 件／主キー制約 0 件／🚨 行は 4 件残っている
   ＝ **中身が残ったまま、開けないコレクションになった**
```
＝ これは「使いにくい」ではなく **「壊れる」**。利用者のデータが取り出せなくなる。

- **画面で口を出さないだけでは足りない**（API を直接叩ける。AGENTS.md 3.5）
- **API が 400 / 409 を返す**。エラーの鍵は**専用に作る**
  （既存を流用すると、画面側で「なぜ駄目か」を言えない）
- **画面でも口を出さない**（API だけだと、画面からは 500 に見える）


### 2-11. 🚨 アイコンだけのボタンは正方形にする

> 原文（2026-08-17）:「縦長のボタンはやめて。**これはルールとして。**
> どう考えても意図的なもの以外アイコンボタンは正方形。」

文字を持たず**アイコンだけ**のボタンは、**縦横を同じにする**。
実例（指摘されたもの）: サイドバーの切替アイコン／「ファイルを追加」の隣の `∨`。

🚨 **§2-6（高さはヘッダーいっぱい）と衝突する。** ヘッダーの高さに合わせると、
幅が足りないアイコンボタンは**縦長になる**。
→ **アイコンボタンだけは、高さに合わせて幅も広げる**（正方形を保つ）。
🚨 「意図的なもの」は例外にできるが、**なぜ正方形でないかを書くこと**。

### 2-12. ボタンの文言は、押すと何が起きるかを言う

> 原文（2026-08-17）:「これはボタン自体が「IDをコピー」と表示するべき。」

「コピー」だけでは**何をコピーするか分からない**。
隣にラベル（`id`）が在っても、**ボタンだけを見た人には伝わらない**。

### 2-13. 🚨 押せない操作は「場所で使い分ける」

> 堀池さんの判断（設問 335）: **C: 場所で使い分ける**

**主な操作（画面の上の方・その画面の目的そのもの）は、押せなくても出す。押せない理由も出す。**
**副次的な操作（`▾` の中・行ごとの操作）は、押せないなら出さない。**

理由は「探させないため」。主な操作が消えると、**利用者は「機能が無い」と読む**（実際は権限が足りない
だけかもしれない）。逆に `▾` の中に押せない項目が並ぶと、**押せるものを探す邪魔になる**。

🚨 **押せない理由は「もう一度お試しください」で済ませない**（§2-9）。
**何が足りないか・誰に頼めばよいか**まで出す。

### 2-14. 「新しく作る」は 2 通りある。どちらを使うかを決めて書く

> 堀池さんの判断（設問 336）: **C: 割れていること自体を規則にする**

**専用ページ**と**一覧の中のフォーム**の**両方を認める**。揃えようとしない。

| 使うほう | いつ |
|---|---|
| **一覧の中のフォーム** | 作るものが**その一覧の一部**で、**入力が少ない**（名前だけ・2〜3 個） |
| **専用ページ** | 作るものが**独立している**、または**入力が多い**・**段取りが要る** |

🚨 **同じ画面の中で 2 通りに割れているのは駄目**（設問 313 でそれを揃えた）。
**画面ごとにどちらかへ倒す。** 迷ったら「その一覧を見ている人が、そのまま足したいか」で決める。

---

## 3. アイコン

### 3-1. 既定に落とさない。項目ごとに設定する

> 原文（2026-08-17）:「ゴミ箱のアイコンが不自然なので、標準的なゴミ箱のアイコンに」
> 原文（2026-08-17）:「**設定のアイコンもちゃんと個別に設定**」

分岐の表に無い項目が既定（`<TableIcon />`）に落ちると、**関係ない画面が表のアイコンになる**。
🚨 **1 件直したら、既定に落ちている項目を全部数える。** 実測（2026-08-17）で
**左サイドバー 35 行中 23 行**が既定に落ちていた。

🚨 **数えるときは組を全部開いてから。** 閉じた組の中身は DOM に出ない
（＝ 閉じたまま数えると「無い」と読める。司令塔が実際に踏んだ）。

### 3-2. 新しい絵を選ばない。既にこのリポジトリで使われているものから採る

### 3-3. 人は絵文字、ものは線画

アバターは絵文字（`avatar-is-emoji-not-initials`）、ナビ・コレクションは lucide の線画。
🚨 この割り方を混ぜない。


### 3-4. 一覧にもアイコンを添える

> 原文（2026-08-17）:「**ユーザーはアイコンなどもちゃんと表示。**」

人を出す列には**アバター（絵文字）**、ものを出す列には**線画のアイコン**。
文字だけの表にしない。§3-3 の割り方を、ナビだけでなく**一覧にも当てる**。

---

## 4. 一覧と選択

### 4-1. 表は横幅いっぱい。左右に余白。端はグラデーションで消す

> 原文（2026-08-17）:「一番左と一番右には余白がちゃんとできるように。横線は横幅いっぱいまで引きますが、
> 中身の部分はスクロールができることが視覚的に伝わるよう、端がグラデーションで消えるように」

### 4-2. クリックは選択。開くのはダブルクリックか、明示のボタン

> 原文（2026-08-17）:「クリックしたらすぐに表示されるのではなく、ダブルクリックで表示するか、
> 詳細アコーディオン内の拡大ボタンから表示する形にして、クリック操作で複数選択ができるように」

### 4-3. 覆い（ライトボックス）は仮想ページの中だけに出す

> 原文（2026-08-17）:「画面全体ではなく、仮想ページの中（左サイドバー、右サイドバー、ヘッダーを
> 除いた領域）だけに表示し、中身だけ暗くオーバーレイがかかるイメージに」

### 4-4. 右クリックは自前のメニューだけ。ブラウザ既定を出さない

> 原文（2026-08-17）:「この仮想ページの中身はすべて右クリックを無効化し、こちらが用意した
> 右クリックメニューのみが表示されるように（モバイルも同様に対応）」

🚨 **仮想ページの中だけに閉じる。** 画面全体で `contextmenu` を殺さない。

---

## 5. ナビゲーション

### 5-1. 畳んだサイドバーのアイコンも押せること

> 原文（2026-08-17）:「左サイドバーをとじたらクリックできない。アコーディオンはクリックしたら
> 左サイドバーを開くか、最初の一つのページに遷移するか、パネルを出すようにして」

吹き出しが出るのに押せない、を作らない。

### 5-2. 同じ語を 2 箇所に出さない

> 原文（2026-08-17）:「admin/files ページ上部の『ファイル』というタイトルは不要です。
> パンくずリストで表示されているためです」

🚨 消す前に、**その要素が別の役目も兼ねていないか**を見る。
実例: ページ内パンくずは「フォルダ階層への唯一の移動手段」も兼ねていた（ヘッダーのパンくずは
`pathname` からしか作らないので、クエリに居るフォルダを載せられない）。

### 5-3. 階層は罫線文字ではなく、描いた線で表す

> 原文（2026-08-17）:「現在の罫線はただの記号のようなので、デザインとしてあしらいを加えて。
> 以前お渡ししたYouTubeのチャットUIのように、角丸の線で動的に変化するイメージに」

`├` `└` `│` は**文字なので読み上げに乗り、字幅がフォントで変わる**。
規約は `knowledge/decisions/tree-connector-lines.md`（肘と通し線を分けて描く）。


### 5-4. 🚨 「戻る」でアプリの外へ出さない

> 由来: 2026-08-17・header が「初めて触る人の目で 1 周」して見つけた。堀池さんの指摘ではない。

**「戻る」はブラウザの履歴であって、画面の階層ではない。**
直接来た人（履歴が無い人）には、**階層の親へ**行かせる。

実測:
```
RED  新しいタブで /admin/version を直接開く（ブックマーク・リンク・再読み込み）
     history.length 2 ／「もどる」を押すと URL が about:blank
     ＝ 🚨 真っ白な画面へ出て、戻る道も無い
     🟢 対照 パンくずの中には /admin への本物のリンクが在った
        ＝ 上へ行く道は在るのに、目立つボタンの方が外へ出していた
GREEN ① 直接開いた人 … もどる後 /admin/collections（/admin の転送先）
      ② 歩いてきた人 … もどる後 /admin/collections（履歴どおり・従来と同じ）
🟢 対照 ② が変わっていない ＝ アプリの中を歩いてきた人の挙動は壊していない
```

- 🚨 **押せない区画（`navigable: false`）を行き先にしない**（`/admin/settings` はページが無く 404）
- 🚨 **これは「アプリの中を歩いてきた人」には見えない**。リンク・ブックマーク・再読み込みで
  直接来た人にだけ起きる。**§1-11 と同じ性質**（歩いてきた人には見えない問題）

---

## 6. 文言

### 6-1. リテラル禁止（英語も）

`app/**` `components/**` の表示文言を直接書かない。`i18n/messages/<locale>/<ns>.json` に ja / en 両方。
検査は `check-i18n-{hardcoded,keys,usage}.mjs`。**詳細は `AGENTS.md` §3.8。**

### 6-2. 利用者が付けた名前は、翻訳できること

> 原文（2026-08-17）:「そもそもコレクションは翻訳するべきです。すべてのコレクションを翻訳できるべき」
> 原文（設問 318 の備考）:「directusを見習って。基本全てに翻訳をつけれる」

対象は **コレクションと欄（フィールド）の 2 つ**。Directus も型で `translations` を持つのは
`collection.ts` と `fields.ts` の 2 本だけ（実測。ロール・ポリシー・利用者には無い）。
🚨 **名前が無いときは識別子をそのまま出す。**空にしない・推測で日本語にしない。

### 6-3. 🚨 「あなたには見えていないだけ」を伝える。ただし専門用語を使わない

> 堀池さんの判断（設問 337）: **B: 絞り込まれていることを返し、文言を変える**
> 備考（原文）: 「**ログインしている前提なので文言を表示していいが、専門用語は使わない**」

権限で絞り込まれて一覧が空のとき、**「0 件です」で終わらせない**。
**「見えていないものがある」と分かる文言**にする。

🚨 **使わない語**: ポリシー / フィルタ / スコープ / 権限セット / ロール（内部の言葉）
✅ **使う語**: 「あなたに見えるものだけを出しています」「見えていないものがあります」
「管理者に確認してください」

🚨 **「見えない」と「無い」を同じ文言にしない。** 利用者は「無い」と読むと**探すのをやめる**。

---

## 7. 画面を直したと言う前に

🚨 **「直しました」は受け取られない。数で出す。**

1. **母集合を 1 行で書く**（何を含み、何を含まないか）
2. **その母集合から外れる作り方を 1 つ挙げる**（挙げられないなら、まだ数えるところではない）
3. **RED と GREEN を両方出す**
4. **対照を並べる**（効かないはずの側で 0 が出ること ＝ 計器が 0 も 1 も出せる）
5. **「0 件」は「異常が無い 0」か「見ていない 0」かを書く**

### 🚨 画面を測るときに実際に踏んだ罠（司令塔が 1 日で 8 回）

| 罠 | どう外したか |
|---|---|
| 語で要素を探す（`aria-label` に breadcrumb が在ると思った） | 実際の値は「現在の場所」。**位置と役割で探す** |
| `[0]` を押す | 別の要素だった。**狙って選ぶ**（角丸の有無・y 座標など） |
| 閉じた状態で中身を数える | アコーディオン・組の中身は**開くまで DOM に出ない** |
| `accordion-content` の背景を読む | 背景は `accordion-item` 自身に付いていた |
| `el.click()` で Radix のメニューを開く | Radix は `pointerdown` を聞いている（`pointerdown` → `pointerup` → `click`）|
| `[role=menu]` だけで中身を探す | `[data-radix-menu-content]` `[data-slot=dropdown-menu-content]` も見る |
| `h1` が残っているのを「消えていない」と読む | **幅 1 × 高さ 1 の読み上げ専用**だった |
| サーバが壊れているのに 0 件を数える | `health` が 500。**何を数えても 0** になる |


### 7-1. 🚨 PC 幅だけで測らない。狭い画面で「組み合わせ」が壊れる

> 由来: 2026-08-17・files が 2 周目を幅 390 でやり直して見つけた。
> 本人の申告:「私はこれまで、自分の画面をほぼ PC 幅（1440）でしか測っていませんでした。
> **それが私の穴でした。**」

```
幅 390 でファイルを 1 枚選ぶ … ダイアログ 1 ／ body と main の pointer-events が none
                              ＝ 🚨 一覧が触れない → 2 枚目を選べない
🟢 対照 幅 1440 …………… ダイアログ 0（aside が開く）／ pointer-events は auto
```
**2 つの正しい指示が、狭い画面で衝突した**（「クリックで複数選択」と「選んだらパネルを開く」）。
🚨 **どちらの実装も指示どおり。組み合わせで初めて壊れる。**

- **PC で測ったら、必ず SP（390 前後）でも測る**
- 🚨 **PC と SP で別の器を使っている所**（右サイドバー ⇄ 全画面ダイアログ など）は、
  **必ず両方で通す**。器が変わると、覆い・当たり判定・スクロールが全部変わる


### 7-2. 🚨 連続で回した中の 1 枚は、報告の前に単独で測り直す

> 由来: 2026-08-17・schema が同じ形を 1 日に 3 回踏み、3 回とも報告前に自分で拾った。

```
1 周目 … collections 一覧で「表の器 null」＝ **表が無い**と出た
確かめた … 単独で測り直すと table-container 1 / 表 1 / 幅 358 / 溢れ true
＝ 🚨 連続で 5 画面を回した中の 4 枚目で、**描き終わる前に測っていた**
   ＝ 「SP では表が消える」と報告する 1 歩手前だった
```

**画面を続けて回すと、後ろの方ほど「無い」が出やすい。**
🚨 **0 件・null・見つからないを報告する前に、その 1 画面だけを単独で開いて測り直す。**
（これで 1 日に 3 件、誤報を止めている。）


### 7-3. 🚨 合っている数を出すと、その内訳も測ったように読まれる

> 由来: 2026-08-17・**files（L4）** が自分で名乗り出た。本人の言葉:
> 「私は今日『人の報告の空欄を推測で埋めない』と書いた側です。**自分の数でそれをやりました。**
> 数（10）が正しかったので、**内訳も測ったように読めてしまいます**。そこがいちばん悪いところです。」
>
> 🚨 **司令塔は最初この引用を「auth の言葉」として規約に入れた。誤りだった。**
> auth はそれを引用して同意しただけで、**言葉の持ち主は files**。
> auth 本人の指摘:「規約に『誰の実測か』を載せる日に、**帰属が 1 つ入れ替わる**と、
> 次に読む人は『auth が数を推測した』と読みます。**していません**。
> 逆に **files が自分で名乗り出た**という、いちばん効いた動きが消えます。」
> ＝ 🚨 **この規約自身が、この規約の対象になった。**

**数だけ測って内訳を推測したなら、「内訳は推測です」と書く。**
🚨 **数が合っていることは、内訳が合っていることを意味しない。**
読む側は、合っている数を見た時点で**内訳も信じる**。


### 7-4. 🚨 失敗の戻り先を変えたら、その戻り先が理由を出せるかを画面で見る

> 由来: 2026-08-17・auth が実測して見つけた（7ad6531f「戻り先を変えたら、失敗の理由が画面から消えていた」）。

**URL に鍵が付いていることは、画面に文言が出ていることの証拠ではない。**
戻り先の画面が、その鍵を読んで文言に変える仕組みを持っているとは限らない。

- 失敗の戻り先を変えたら、**その画面を実際に開いて文言を見る**
- 🚨 検査を作るなら **過検出に倒す**（呼んでいなければ落とす）。
  取りこぼすと気づけないが、過検出なら人が 1 件見に行くだけで済む


### 7-5. 🚨 「その語が何回出るか」を受入基準にしない

> 由来: 2026-08-17・files が作業者へ振ったときに起きた。本人の申告:
> 「私は**数を測る側の規律**は守っていましたが、**数を要求する側の危なさ**を見ていませんでした。」

置いた受入基準:
```
・grep -n "deletePrefix" が 2 箇所
・grep -c "storage.delete(key)" が 0
```
返ってきたもの（**頼んでいない 3 つの変更**）:
```
① コメントの中の deletePrefix を「prefix 削除」に書き換えた（4 行）
   ＝ 過去の事故の記録の語が変わり、**次の人が grep で辿れなくなる**
② await storage.delete(key)
   → const objectKey = key; await storage.delete(objectKey);
   ＝ **意味の無い変数を挟んだだけ**。目的は文字列を消すこと
③ 🚨 await storage.deletePrefix?.(...)
   → const f = Reflect.get(storage, "delete" + "Prefix"); await f?.call(...)
   ＝ **メソッド名を文字列で分解**して grep に当たらない形にした
     （この呼び出しは、今後 deletePrefix を探しても永久に出てこない）
```
🚨 **どれも受入基準を満たす。そしてどれもコードを悪くしている。**
作業者は**コードの意味ではなく、数を合わせに来る**（悪意ではなく、それが基準だから）。

→ **差分そのものを貼らせて、依頼者が 1 行ずつ読む。**
🚨 とくに危ないのは「**0 件であること**」を求める形——**消す・隠す・言い換える**動機を作る。

### 7-6. 🚨 「表示と編集を分ける」が掛かるのは、見る値が在る画面だけ

> 堀池さんの判断（設問 312）: **A: その理解で合っている**

**表示モードが存在しえない画面は、そもそも「分ける」対象ではない。**

| 対象か | 画面 | なぜ |
|---|---|---|
| ❌ 対象外 | **新しく作る画面** | 新規なので空。**表示モードが作れない** |
| ❌ 対象外 | **一覧＋その場で足すフォーム** | 一覧は既に表示・フォームは常に「作る」側 |
| ❌ 対象外 | **コメントの投稿欄** | 投稿するための欄で、既存の値ではない |
| ✅ 対象 | **既存の値を見る画面** | ここだけが「表示と編集の両方を持ちうる」 |

🚨 **これは例外を作る話ではない。** 設問 287 の「例外なし」はそのまま生きていて、
**掛かる相手がどれかがはっきりしただけ**。

### 🚨 「役目が重なっている」と思ったら、中身を引いてから決める

2026-08-17、**2 人が同じ日に、同じ形で間違えた**（どちらも自分で気づいて訂正した）。

```
見立て … 「⌘K の『この画面の中』タブと、ヘッダーの検索窓は役目が重なる。タブを外そう」
実測 …… **別物だった**
   ⌘K のタブ … **全体検索の結果を、いまの画面に関係する種類だけに絞る**
   ヘッダーの窓 … **その画面に並んでいる行を絞る**（collections で ?q=zz → 16 行 → 6 行）
   ＝ 同じ「絞る」でも、**絞る対象が違う**（結果の種類 / 画面の行）
```

🚨 **見た目や名前が似ていることは、役目が同じことを意味しない。**
片方を消す判断をする前に、**両方の中身を引く**。
（消したあとで「別物だった」と分かると、消した側を作り直すことになる。）

---

### 🚨 確認に「無条件に出る文字」を使わない

```
❌ echo "(空=復元できた)"        … 🚨 **成否に関わらず必ず出る**。2 回騙されかけた（1 回は実際に失敗していた）
✅ コマンドの実出力・終了コードで確かめる
```
自分で書いたラベルは、**自分が期待した通りに読める**。だから嘘に気づけない。

---

**ログインは `POST /api/auth/dev-login?admin=true` ＋ body `{"email":"…"}`。**
🚨 `?admin=true` が無いと管理者でない利用者ができて、画面が「まだ許可されていません」になる。

---

## 8. このファイルの育て方

- 堀池さんから画面の指摘が来たら、**原文を引用して**ここに 1 項目足す
- **画像で指摘が来たら、見たままを文字にして残す**（次に読む人は画像を見られない）
- 前の指示と衝突して見えるときは、**両方を並べて「何が違うか」を書く**（§1-3 がその形）
- 消さない。**反転したなら経緯を残す**

/**
 * 画面内でファイルを掴んで運ぶときに使う種類（MIME）。
 *
 * 🚨 **`text/plain` を使わない。** 素の文字列で運ぶと、**外から来たテキストのドロップと
 *    区別が付かない**（メモ帳から文字を放り込まれても、こちらは「ファイルを運んできた」と
 *    読んでしまう）。独自の種類にしておけば、`dataTransfer.types` を見るだけで
 *    「画面内から掴んだものか」が分かる。
 *
 * 🚨 **アップロードの層と棲み分けるための鍵でもある。** 外からのファイルは `Files`、
 *    画面内のファイルはこの種類。取り違えると、**移動しようとしただけでアップロードが始まる**。
 *
 * 中身は**ファイル ID の配列を JSON にしたもの**（1件でも配列）。将来まとめて運べるようにするため、
 * 最初から配列にしてある。
 */
export const DRAG_FILE_MIME = "application/x-ohmycms-file-ids";
